Design folder (snapshot)

This directory contains a quick snapshot of the current frontend assets so you can:

- Inspect the live CSS used by the app: `design/css/app.css`
- Inspect the live JS used by the app: `design/js/app.js`

Notes:
- These are copies of `backend/assets/*` and intended for visual inspection or iteration.
- If you want a full design workflow (SCSS sources, build step, or a Figma export), I can scaffold SCSS partials and a small npm-based build.
